import styled from 'styled-components'

export const AudioToggler = styled.span`
    display: flex;
    width: ${props => props.size}px;
    height: ${props => props.size}px;
    position: relative;
`